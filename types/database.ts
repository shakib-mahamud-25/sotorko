/**
 * Placeholder for Supabase-generated database types.
 *
 * Once the schema in docs/02-Architecture.md §4 is migrated into a
 * real Supabase project, regenerate this file with:
 *
 *   npx supabase gen types typescript --project-id <id> > types/database.ts
 *
 * Using `any` here is intentional and temporary — every other part
 * of the codebase should import domain types from `@/types` instead,
 * which are hand-written and already match the architecture doc.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
